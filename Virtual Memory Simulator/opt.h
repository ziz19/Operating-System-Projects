#include <stdio.h>

void execute_opt();   // execute the clock algorithm
void init_opt(FILE* fd, int num_of_frames,FILE* cpy);  // initialize with file, number of pointers, a copy of the same file
void print_opt_stats();
void exit_opt();